<!DOCTYPE html>
<html>
  <head>
    <title>Confederation27</title>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'favicon.ico' %>
    <meta name="viewport" content="width=device-width, intial-scale=1">
  </head>
  <body>
    <div class="navbar">
      <div class="navbar-inner">
        <%= link_to("<i class='icon-home'></i>".html_safe, root_path, :class => "brand") %>
        <ul class="nav">
          <li class="divider-vertical"></li>
          <li><%= link_to("Play", play_songs_path) %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to("Search", root_path) %></li>
          <li class="divider-vertical"></li>
          <li><%= link_to("Most played 5", songs_path) %></li>
        </ul>
      </div>
    </div>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
